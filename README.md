A lightweight HTTP/1.1 web crawler written in python. Will authenticate, maintain session state, and recursively traverse a large website using raw socket programming.

## Usage

```bash
crawler -s <server> -p <port> <username> <password>
```
